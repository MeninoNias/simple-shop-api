import { createUser, getAllUsers, createUserValidation, loginUserValidation, loginUser } from './UserController';


export const UserController = {
    createUser,
    getAllUsers,
    loginUser,
    createUserValidation,
    loginUserValidation
};